package editor_test

import (
	"testing"
)

func TestSelector(t *testing.T) {

	t.Run("should register selectors and return them", func(t *testing.T) {
		// Given
		// s := editor.NewSelector()

		// When
		// s.RegisterEditor()
	})

}
