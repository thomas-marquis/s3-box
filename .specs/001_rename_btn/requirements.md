# Requirement: add a "rename" button for files and directories

## Specifications

* Add a new button in the details panel (for both files and directories) alongside the other existing buttons
* Respect the existing architecture and coding convention and add tests
* Don't add the button for root folder (/)
* Disable this button if the connection is in read-only mode